# Implementation Plan - Automate Announcements

The goal is to make the "Announcements" (お知らせ) section work like the "Blog" section: centralized data management via a script that scans HTML files, allowing for easy updates by simply adding new HTML files.

## Proposed Changes

### Data and Automation
#### [MODIFY] [update-blog-index.ps1](file:///c:/Users/maiku/.gemini/antigravity/update-blog-index.ps1)
- Rename to `update-content.ps1` (or keep name and expand functionality).
- Add logic to scan for `news*.html` files.
- Extract title and date (creation time or existing) from `news*.html`.
- Generate `announcements.js` similarly to [blog-posts.js](file:///c:/Users/maiku/.gemini/antigravity/blog-posts.js).

#### [NEW] [announcements.js](file:///c:/Users/maiku/.gemini/antigravity/announcements.js)
- Will contain `const announcements = [...]`.
- Auto-generated by the script.

### Frontend
#### [MODIFY] [announcements.html](file:///c:/Users/maiku/.gemini/antigravity/announcements.html)
- Remove hardcoded announcement cards.
- Add `<div id="announcement-list-container">`.
- Import `announcements.js`.
- Add JS logic to render the list (same logic as [blog.html](file:///c:/Users/maiku/.gemini/antigravity/blog.html)).

#### [MODIFY] [index.html](file:///c:/Users/maiku/.gemini/antigravity/index.html)
- (Optional but recommended) Add a "Latest News" section or ensure the link to [announcements.html](file:///c:/Users/maiku/.gemini/antigravity/announcements.html) is prominent.
- Current request focuses on "Announcements section", so I will focus on [announcements.html](file:///c:/Users/maiku/.gemini/antigravity/announcements.html).

## Verification Plan

### Automated/Script Tests
1. **Create Dummy Announcement**: Create `news_test.html`.
2. **Run Script**: Execute the updated PowerShell script.
3. **Verify JS**: Check content of `announcements.js` to ensure `news_test.html` is indexed correctly.

### Manual Verification
1. **Open [announcements.html](file:///c:/Users/maiku/.gemini/antigravity/announcements.html)**: Check if the list renders the dummy announcement.
2. **Check Links**: Ensure the link goes to `news_test.html`.
